# Models

This directory stores trained model artifacts (e.g., serialized pipelines and evaluation metrics). The folder is ignored by version control except for key files like `best_model.joblib`, `metrics.json`, and this note.
